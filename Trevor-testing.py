try:
    value = int(input('Enter an integer: 0'))
    print('The inverse of', value, 'is', 1/value)
except:
    print('You Did Not Provie me with a value, so this will not Work!!')

value = int(input('Enter an integer: '))
print('The inverse of',value, 'is', 1/value)